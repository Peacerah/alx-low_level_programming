#include <stdio.h>

/**
 * main - main entry
 *
 * Programm to return capital and small letter
 *
 * Return: Return 1 or 0
 */



int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
		return (0);
}
